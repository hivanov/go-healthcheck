Create a Microsoft SQL Server health checker, using as template the Postgres checker:
Make sure that the library depends on the official Microsoft SQL Server client library for Go. Make sure to install all 
dependant drivers (ODBC, etc.) where needed.
